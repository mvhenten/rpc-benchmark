# -*-perl-*-

package.MvhentenRPCExperiment = {
    interfaces = (1.0);

    deploy = {
        generic = true;
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = npm-pretty-much;
    build-tools = {
        1.0 = {
            NpmPrettyMuch = 1.0;
            NodeJS = 8.x;
        };
    };
};
